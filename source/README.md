# Product Source Workspace

This repository is both the product source and the reviewer-first test project, so normal Neuron work should use the repository root as the source checkout.

Keep this directory empty unless the project is later split back into a standalone test repository. If that happens, use `source/neuron/` for a long-lived local checkout or submodule and do not commit product source contents into the test project.
